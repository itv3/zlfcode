# ZLF Code 7.4.5-v0.02

## 主要变更

- 修复选择候选模型后仍保留自动匹配参数的问题，候选模型的能力、token limit、成本和完整推理变体现在会正确覆盖自动默认参数。
- 支持在应用候选模型参数后继续手动调整，并以保存时的最终表单参数为准。
- 修复候选预览包含 `Max` 推理强度，但添加后的模型缺少 `Max` 的问题。
- 同步更新 VS Marketplace、Open VSX 和 GitHub 文档中的默认参数匹配说明。
- 市场版本更新为 `7.4.502`，发布批次和 VSIX 文件名使用 `7.4.5-v0.02`。
