# ZLF Code 7.4.5-v0.03

## 主要变更

- 修复 GPT-5.6 模型选择 `Max` 推理强度后实际请求仍使用 `Medium` 的问题。
- 为 GPT-5.6 补充完整的 `Max` 推理变体，请求会正确传递 `reasoningEffort: "max"`。
- 将兼容改动限制为一条带 `kilocode_change` 标记的 GPT-5.6 版本条件，降低后续合并上游更新时的冲突风险。
- 市场版本更新为 `7.4.503`，发布批次和 VSIX 文件名使用 `7.4.5-v0.03`。
