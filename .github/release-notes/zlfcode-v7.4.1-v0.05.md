# ZLF Code 7.4.1-v0.05

## 主要变更

- 修复输入框中通过 `@文件` 选择相对路径文件后，附件偶发解析成根目录路径（如 `/README.md`）导致发送失败的问题。
- 文件附件改由扩展端按实际会话目录解析，覆盖普通聊天、命令发送和云会话导入发送路径。
- 文件搜索在后端短暂不可用时不再返回空工作目录，前端也会忽略关闭候选窗口后的迟到搜索结果。
- 市场版本更新为 `7.4.105`，发布批次和 VSIX 文件名使用 `7.4.1-v0.05`。
