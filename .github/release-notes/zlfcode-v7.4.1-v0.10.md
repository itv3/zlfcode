# ZLF Code 7.4.1-v0.10

## 主要变更

- 修复历史会话恢复时，provider 尚未加载完成或对应 provider 已删除后，聊天模型选择器仍可能显示旧模型 ID 的问题。
- 在 provider 列表和智能体列表可用后重新恢复会话偏好，只恢复当前仍有效的模型选择。
- 模型选择器在 provider 数据已加载后不再使用失效的原始 provider/model ID 作为显示兜底，避免用户误以为已删除模型仍可用。
- 市场 Overview 中的发布批次和市场版本同步更新为 `7.4.1-v0.10` / `7.4.110`。
